import React from 'react';
import Page from '../components/Page';

const Home = () => {
  return (
    <div
      style={{
        height: '100vh',
        border: '1px solid black',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontFamily: 'chomsky',
        fontSize: '300px',
      }}
    >
      The Front Page
    </div>
  );
};

export default Home;
